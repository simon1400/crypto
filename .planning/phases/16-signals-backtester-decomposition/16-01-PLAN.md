---
phase: 16-signals-backtester-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/signals/SignalModal.tsx
  - frontend/src/components/signals/DepositSimulator.tsx
  - frontend/src/components/signals/StrategyAnalysis.tsx
  - frontend/src/pages/Signals.tsx
autonomous: true
requirements: [SIG-01, SIG-02, SIG-03]

must_haves:
  truths:
    - "SignalModal renders signal details with TP levels, chart, and metadata"
    - "DepositSimulator simulates compound deposit growth from signal history"
    - "StrategyAnalysis shows leverage verdicts, R:R math, and recommendations"
    - "Signals.tsx imports and uses extracted components identically to before"
  artifacts:
    - path: "frontend/src/components/signals/SignalModal.tsx"
      provides: "Signal detail modal with TP grid, chart, metadata"
      exports: ["default"]
    - path: "frontend/src/components/signals/DepositSimulator.tsx"
      provides: "Deposit simulation with trade log and stats"
      exports: ["default"]
    - path: "frontend/src/components/signals/StrategyAnalysis.tsx"
      provides: "Strategy analysis with leverage, direction, TP depth stats"
      exports: ["default"]
    - path: "frontend/src/pages/Signals.tsx"
      provides: "Signals page orchestrator importing extracted components"
  key_links:
    - from: "frontend/src/pages/Signals.tsx"
      to: "frontend/src/components/signals/SignalModal.tsx"
      via: "import default + JSX usage"
      pattern: "import SignalModal from.*components/signals/SignalModal"
    - from: "frontend/src/pages/Signals.tsx"
      to: "frontend/src/components/signals/DepositSimulator.tsx"
      via: "import default + JSX usage"
      pattern: "import DepositSimulator from.*components/signals/DepositSimulator"
    - from: "frontend/src/pages/Signals.tsx"
      to: "frontend/src/components/signals/StrategyAnalysis.tsx"
      via: "import default + JSX usage"
      pattern: "import StrategyAnalysis from.*components/signals/StrategyAnalysis"
---

<objective>
Extract SignalModal, DepositSimulator, and StrategyAnalysis from Signals.tsx into dedicated component files under `components/signals/`.

Purpose: Signals.tsx shrinks from 888L to ~400L by moving 3 self-contained inline components to their own files. Zero functional changes.
Output: 3 new component files + slimmer Signals.tsx orchestrator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/pages/Signals.tsx
@frontend/src/components/SignalBadge.tsx
@frontend/src/components/SignalChart.tsx
@frontend/src/lib/formatters.ts

<interfaces>
<!-- Key types the executor needs from existing code -->

From frontend/src/api/client.ts (Signal type used by all 3 components):
```typescript
export interface Signal {
  id: number
  coin: string
  type: 'LONG' | 'SHORT'
  leverage: number
  entryMin: number
  entryMax: number
  stopLoss: number
  takeProfits: number[]
  status: string
  channel: string
  publishedAt: string
  entryFilledAt?: string
  statusUpdatedAt?: string
}
```

From frontend/src/lib/formatters.ts:
```typescript
export function formatPrice(price: number): string
```

From frontend/src/components/SignalBadge.tsx:
```typescript
export default function SignalBadge({ status, type }: { status: string; type: string }): JSX.Element
```

From frontend/src/components/SignalChart.tsx:
```typescript
export default function SignalChart({ signal }: { signal: Signal }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SignalModal, DepositSimulator, StrategyAnalysis into components/signals/</name>
  <read_first>
    - frontend/src/pages/Signals.tsx (full file -- lines 1-888)
    - frontend/src/components/SignalBadge.tsx
    - frontend/src/components/SignalChart.tsx
    - frontend/src/lib/formatters.ts
  </read_first>
  <files>
    frontend/src/components/signals/SignalModal.tsx
    frontend/src/components/signals/DepositSimulator.tsx
    frontend/src/components/signals/StrategyAnalysis.tsx
  </files>
  <action>
Create directory `frontend/src/components/signals/`.

**SignalModal.tsx (SIG-01)** -- extract lines 57-153 from Signals.tsx:
- Move the `SignalModal` function component as-is (no logic changes)
- Props interface: `{ signal: Signal; onClose: () => void }`
- Imports needed: `Signal` from `../../api/client`, `SignalBadge` from `../SignalBadge`, `SignalChart` from `../SignalChart`, `formatPrice` from `../../lib/formatters`
- `export default function SignalModal(...)`
- Keep the `entry = (signal.entryMin + signal.entryMax) / 2` calculation inside the component

**DepositSimulator.tsx (SIG-02)** -- extract lines 155-345 from Signals.tsx:
- Move BOTH the `SimTrade` interface (lines 155-165) and the `simulateDeposit` helper function (lines 167-224) into this file as module-level non-exported items
- Move the `DepositSimulator` function component (lines 226-345) as the default export
- Props interface: `{ signals: Signal[] }`
- Import: `Signal` from `../../api/client`, `useState` from `react`
- `export default function DepositSimulator(...)`

**StrategyAnalysis.tsx (SIG-03)** -- extract lines 347-490 from Signals.tsx:
- Move the `StrategyAnalysis` function component as-is
- Define the stats prop interface at the top of the file as a named interface:
```typescript
interface StrategyStats {
  avgWin: number
  avgLoss: number
  winrate: number
  tp2plus: number
  tp: number
  sl: number
  closedCount: number
  leverageStats: Record<string, { wins: number; losses: number }>
  directionStats: { longWins: number; longLosses: number; shortWins: number; shortLosses: number }
}
```
- Props: `{ stats: StrategyStats }`
- No external imports needed beyond React (component is pure UI math)
- `export default function StrategyAnalysis(...)`

All three files: no semicolons, single quotes, 2-space indent per project conventions.
  </action>
  <verify>
    <automated>cd D:/crypto/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/signals/SignalModal.tsx exists with `export default function SignalModal`
    - frontend/src/components/signals/DepositSimulator.tsx exists with `export default function DepositSimulator` and contains `simulateDeposit` function and `SimTrade` interface
    - frontend/src/components/signals/StrategyAnalysis.tsx exists with `export default function StrategyAnalysis` and `StrategyStats` interface
    - All three files import from correct relative paths (../../api/client, ../SignalBadge, etc.)
  </acceptance_criteria>
  <done>Three component files exist in components/signals/ with correct exports, imports, and no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Update Signals.tsx to import extracted components and remove inline definitions</name>
  <read_first>
    - frontend/src/pages/Signals.tsx
    - frontend/src/components/signals/SignalModal.tsx (just created)
    - frontend/src/components/signals/DepositSimulator.tsx (just created)
    - frontend/src/components/signals/StrategyAnalysis.tsx (just created)
  </read_first>
  <files>frontend/src/pages/Signals.tsx</files>
  <action>
Edit Signals.tsx to:

1. **Add imports** at top (after existing imports):
```typescript
import SignalModal from '../components/signals/SignalModal'
import DepositSimulator from '../components/signals/DepositSimulator'
import StrategyAnalysis from '../components/signals/StrategyAnalysis'
```

2. **Remove** these inline definitions:
   - Lines 57-153: `function SignalModal(...)` -- entire component
   - Lines 155-165: `interface SimTrade` 
   - Lines 167-224: `function simulateDeposit(...)`
   - Lines 226-345: `function DepositSimulator(...)`
   - Lines 347-490: `function StrategyAnalysis(...)`

3. **Keep everything else unchanged**: the `exportCSV` function (lines 9-46), `CHANNELS` constant (lines 48-55), main `Signals` component (lines 492-888), and the stats computation block (lines 578-639).

4. The JSX in the main component that uses `<SignalModal>`, `<DepositSimulator>`, and `<StrategyAnalysis>` stays exactly as-is -- just now importing from external files instead of inline.

5. Remove the `SignalBadge`, `SignalChart`, and `formatPrice` imports from Signals.tsx ONLY IF they are no longer used after extraction. Check: `SignalBadge` and `SignalChart` are only used inside SignalModal, and `formatPrice` is only used inside SignalModal -- so remove all three imports from Signals.tsx.

Result: Signals.tsx should be ~400 lines (down from 888).
  </action>
  <verify>
    <automated>cd D:/crypto/frontend && npx tsc --noEmit 2>&1 | head -20 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Signals.tsx contains `import SignalModal from '../components/signals/SignalModal'`
    - Signals.tsx contains `import DepositSimulator from '../components/signals/DepositSimulator'`
    - Signals.tsx contains `import StrategyAnalysis from '../components/signals/StrategyAnalysis'`
    - Signals.tsx does NOT contain `function SignalModal(`
    - Signals.tsx does NOT contain `function DepositSimulator(`
    - Signals.tsx does NOT contain `function StrategyAnalysis(`
    - Signals.tsx does NOT contain `interface SimTrade`
    - Signals.tsx does NOT contain `function simulateDeposit(`
    - `tsc --noEmit` passes with zero errors
    - `vite build` succeeds
  </acceptance_criteria>
  <done>Signals.tsx is ~400 lines, imports 3 extracted components, tsc and vite build pass cleanly</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- zero errors
2. `cd frontend && npx vite build` -- successful build
3. Signals.tsx line count under 450 (was 888)
4. All 3 new component files exist in `frontend/src/components/signals/`
</verification>

<success_criteria>
- tsc --noEmit passes
- vite build succeeds
- Signals.tsx reduced by ~50% in line count
- SignalModal, DepositSimulator, StrategyAnalysis live in components/signals/
- No functional changes -- same props, same rendering
</success_criteria>

<output>
After completion, create `.planning/phases/16-signals-backtester-decomposition/16-01-SUMMARY.md`
</output>
